#
# Backend Lit configuration sample
# Copy these values into your real `.env` file before running the backend.
#

# Lit Protocol network to target (cayenne=production, jalapeno=staging, serrano=testnet)
LIT_NETWORK=cayenne

# Hex private key (0x-prefixed) used to authenticate with Lit.
# Generate with: `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`
LIT_PRIVATE_KEY=0xYOUR_LIT_PRIVATE_KEY

# Optional: Pre-generated AuthSig JSON from @lit-protocol/auth-helpers (stringified object)
LIT_AUTH_SIG={"sig":"0x...","derivedVia":"web3.eth.personal.sign","signedMessage":"...","address":"0x..."}

# Optional: Override Lit relay URL if not using the default public relay
# LIT_RELAY_URL=https://your-custom-lit-relay.example.com

# Existing Walrus/Sui env vars go below...

