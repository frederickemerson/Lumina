#
# Frontend Lit configuration sample
# Copy these into `.env.local` (Vite) and adjust as needed.
#

VITE_LIT_NETWORK=cayenne

# Optional: shareable relay endpoint for client-side Lit actions (if different from default)
# VITE_LIT_RELAY_URL=https://your-custom-lit-relay.example.com

# Optional: PKP or Lit Action ID if leveraging programmable signing on the client.
# VITE_LIT_ACTION_PKP_PUBLIC_KEY=0xYOUR_PKP_PUBLIC_KEY

